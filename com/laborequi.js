let subtitleMarkConfig = { color: 'red', size: 14 };

if (!isEmpty(subtitleMarkConfig)) {
  console.log('Subtitle mark configuration is not empty.');
  // Additional code related to non-empty subtitleMarkConfig
} else {
  console.log('Subtitle mark configuration is empty.');
  // Additional code related to empty subtitleMarkConfig
}
